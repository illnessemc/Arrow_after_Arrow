# 星箭迷途

当前版本：`v1.0`

这是一个使用 Python 和 Pygame 开发的网格箭头解谜游戏。当前版本以“密集、
简洁、整条折线逐段抽出”为主要表现：箭头可以占据多个格子并多次转弯，
第 1 关使用稀疏布局教学，正式关和无尽关会铺满全部可玩格。

## 当前功能

- 开始、游戏、通关和失败页面；
- 多格直线箭头与多转角折线箭头；
- 点击箭头任意线段选择整支箭头；
- 箭头沿自身路径像蛇一样逐段前进并飞出棋盘；
- 受阻时点击箭头向前试探，第一支阻挡箭头以专用红色震动提示；
- 箭身使用切线连续的三次曲线圆角和加粗圆形尾端；
- 飞出动画按圆角曲线弧长连续滑动；
- 动画播放时仍可继续点击，合法操作按顺序进入动画队列；
- 箭头使用五倍分辨率整条描边后平滑缩小，减少斜边和圆角毛刺；
- 只在未被箭头占用的可玩格显示棋盘点，动画离开后再显出；
- 飞出时经过的棋盘点会依次放大再恢复，形成低对比度单色拖尾；
- 棋盘尺寸按关卡配置，界面会自动缩放；
- 深色纵向布局和确定性的多色箭头，相邻箭头不会同色；
- 首页、游戏背景、标题、Logo、按钮、设置、生命和结果页已接入星空主题素材；
- 1 个稀疏手工教学关和 5 个固定路径的手工主题关；
- “潮汐回廊”“四向机关”“同心星环”“虹桥织网”“星门终阵”采用不同构图；
- 正式关铺满各自可玩区域，并在载入时完成通关流程验证；
- 固定关不使用随机数，重开或再次进入时布局完全一致；
- 无尽模式每关使用新随机种子在线生成，并在进入游戏前验证可通关性；
- 开始界面可选择任意关卡并提供退出按钮，齿轮或 `Esc` 用于唤出菜单；
- 普通启动不提供调试能力，`--debug` 版本可强制移除箭头和切换关卡。

## 运行方法

建议使用 Python 3.10 或更高版本。

```bash
python -m pip install -r requirements.txt
python main.py
```

需要调试关卡时使用独立启动参数：

```bash
python main.py --debug
```

鼠标左键点击折线箭头。游戏内或开始界面按 `Esc` 打开设置，点击“退出游戏”
或关闭窗口退出。

运行全部自动化测试（包含 T01～T06 和固定关卡目录验证）：

```bash
python -m unittest discover -s test -v
```

## 文件结构

```text
lab2/
├─ main.py                         # 程序入口
├─ requirements.txt                # 运行依赖
├─ test/                           # 规则、流程和固定关卡自动化测试
├─ arrow_game/
│  ├─ app.py                       # Pygame 页面、输入、动态布局和动画
│  ├─ core/                        # 与图形界面无关的游戏核心
│  │  ├─ arrow.py                  # Arrow：箭头及其有序折线路径
│  │  ├─ board.py                  # GameBoard：地图与格子占用索引
│  │  ├─ direction.py              # Direction：四方向及坐标增量
│  │  ├─ game.py                   # GameSession：点击、失误和输赢规则
│  │  ├─ layout.py                 # BoardLayout：矩形/异形棋盘布局
│  │  ├─ level.py                  # Level：关卡模板与完整覆盖校验
│  │  ├─ rules.py                  # 可替换的箭头出界规则
│  │  └─ solver.py                 # LevelSolver：自动流程验证与难度指标
│  ├─ data/
│     ├─ builders.py               # 手工关卡构建器及公共接口
│     ├─ endless.py                # 无尽模式随机种子与生成重试
│     ├─ generator.py              # 仅供无尽模式使用的在线生成器
│     ├─ manual_levels.py          # 五个固定主题关的逐支路径数据
│     └─ levels.py                 # 教学关和固定主题关目录
│  └─ ui/
│     ├─ animation.py              # 连续操作的动画队列与状态快照
│     ├─ assets.py                 # 背景、标题和按钮图片的资源接口
│     └─ theme.py                  # 深色主题及相邻避色算法
└─ resource/
   └─ image/                       # 星空主题背景、标题、按钮和状态素材
```

## 核心设计

### 箭头和地图

`Arrow` 保存从尾部到头部的连续坐标序列，支持任意长度和多个直角转弯。
`GameBoard` 保存“格子 -> 箭头 ID”的占用索引，放入和移除箭头时会统一更新
全部身体格。点击线段任意位置都能找到整支箭头。

阻挡检测只需从箭头头部沿最终方向扫描。每个格子的占用查询为常数时间，
扫描成本与头部到棋盘边界的距离成正比；当前棋盘规模无需增加复杂空间索引。

### 动态棋盘

`BoardLayout` 独立描述棋盘行列数和可玩坐标，界面根据当前尺寸计算格子大小，
并未把玩法限定为固定的 7×9。布局可通过 `playable_cells` 使用缺角、带空洞
或多边形轮廓；当前最后一关使用上窄下宽的“星门”异形棋盘。

### 固定主题关卡

固定流程的五个正式关不再调用自动生成器。`manual_levels.py` 逐支保存箭头的
有序坐标，`HandcraftedLevelSpec` 只负责把已经定稿的路径还原成 `Level`。
这种方式使每一关的构图、依赖和难度在版本内保持稳定，也便于针对某一支箭头
继续人工调整。

- 第 2 关“潮汐回廊”：横向波浪和短折返交替；
- 第 3 关“四向机关”：短折线从四个方向交错制约，减少长直线；
- 第 4 关“同心星环”：外环与内环层层嵌套；
- 第 5 关“虹桥织网”：横向桥梁与纵向折返互锁；
- 第 6 关“星门终阵”：使用上窄下宽的异形轮廓，综合四向依赖。

关卡界面仍只显示“第 N 关”，主题用于关卡设计和代码维护，不增加游戏内文字
注解。自动生成器及其形状、长度和依赖质量门槛继续保留，但只服务无尽模式。

### 全覆盖和流程验证

关卡创建时会检查越界、路径断裂、格子重叠和重复 ID。正式关及无尽关使用
`CoverageMode.REQUIRE_FULL`，每个可玩格必须恰好属于一支箭头；第 1 关显式
使用 `CoverageMode.ALLOW_EMPTY`，用较少箭头突出基础阻挡关系。

`LevelSolver` 使用与正式游戏相同的规则尝试清空棋盘。当前基础规则具有单调性：
移除箭头只会减少阻挡，所以每步选择任意可飞出的箭头即可完成可靠验证；若某
一步没有可选箭头，关卡会在载入前被拒绝。求解报告还记录步骤数和每步可选数，
为后续关卡排序及难度曲线预留数据。

### 手工固定关

第 1 关“方向入门”是 9×8 稀疏手工关，6 支箭头形成清晰的教学依赖链。
后续 5 关尺寸分别为 16×12、18×14、20×16、22×16 和 24×18，全部使用固定
路径并由求解器验证；第 6 关只启用星门轮廓内的可玩格。所有正式关均完整覆盖
可玩区域，无空位、无重叠。

### 无尽模式

`EndlessLevelFactory` 在玩家点击“无尽模式”或完成当前无尽关后才生成下一关。
每轮从系统随机源取得新种子，候选仍经过完整覆盖、质量策略和 `LevelSolver`
验证；单个种子未达标时自动换种子，达到尝试上限则显示可重试提示，不会让
程序崩溃。棋盘尺寸随轮次缓慢增加并封顶为 30×20，以控制生成时间和内存。

### 规则扩展

`GameSession` 负责点击、失误次数和输赢状态；出界判断通过 `ExitRule` 注入，
当前仍使用基础的 `StraightExitRule`。以后增加特殊出口、转向、机关或新的
棋盘形状时，可以替换规则或生成器，而不用把逻辑堆进 Pygame 界面层。

### 界面资源扩展

`GameTheme` 集中保存界面颜色，箭头配色由邻接图自动分配，相邻箭头不会使用
同一种颜色。`AssetProvider` 为开始、选关、设置、游戏、生成中、结果和失败页
背景，以及标题、Logo、设置图标、生命、星星、通关图标、失败图标和按钮保留
语义资源键。`DirectoryAssetProvider` 默认从 `resource/image` 加载并缓存素材，
自动裁掉透明留白；缺少单项素材时仍会回退到文字和 Pygame 图元。

应用退出时会统一清空动画、颜色表和素材缓存，再调用 `pygame.quit()`。
重新开始或切换关卡会用新 `GameSession` 替换旧棋盘并清空表现状态。固定关只
从不可变路径数据重建；无尽模式只保存当前关，下一关替换上一关，不缓存历史
地图。程序不会启动网络服务或占用端口。

### 连续操作与动画

核心规则在点击时立即更新棋盘，`AnimationQueue` 保存对应的表现快照并按点击
顺序播放。后续合法箭头无需等待当前动画结束即可继续点击；尚未播放的箭头仍
保持静态显示。阻挡动画会保存点击瞬间的第一支阻挡箭头快照，并以普通配色中
不存在的醒目红色震动显示；排队期间会锁定被点击箭头，防止快速重复点击被多
次扣除机会。重新开始、切关或返回首页时会同时清空动画状态。

## 固定关与无尽关的数据策略

六个固定关选择离线保存：教学路径位于 `levels.py`，五个主题关路径位于
`manual_levels.py`。启动时只进行轻量的数据校验和求解验证，不再为固定关执行
随机候选搜索；重新开始或再次进入关卡也不会改变布局。

无尽关选择在线生成：仅在玩家进入无尽模式或继续下一轮时取得新随机种子，
通过完整覆盖、质量策略和 `LevelSolver` 验证后才进入游戏。新一关会替换上一关
的运行时引用，不写入磁盘，也不会随着游玩轮次持续占用存储空间。
